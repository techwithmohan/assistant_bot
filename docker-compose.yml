version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: assistant-bot
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./assistant.db:/app/assistant.db
      - .env:/app/.env
